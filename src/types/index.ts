import type { EnfyraClientConfig } from '@enfyra/sdk-core';

export type VueClientConfig = string | EnfyraClientConfig;

export type {
  AuthLoginResult,
  AuthTokens,
  EnfyraClientConfig,
  EnfyraError,
  FileRecord,
  FilterObject,
  FolderNode,
  LoginCredentials,
  MutationOperation,
  MutationOptions,
  MutationParams,
  QueryOptions,
  RequestStatus,
  StorageConfigRecord,
  UserInfo,
  WebSocketConfig,
} from '@enfyra/sdk-core';
